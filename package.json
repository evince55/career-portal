{
  "name": "career-portal",
  "version": "1.0.0",
  "description": "Content-first portfolio with a live homelab dashboard — zero-dependency vanilla JS PWA",
  "type": "module",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "build": "npm run lint && npm run test && cp -r js css scripts config icons screenshots .github dist/ && cp index.html manifest.json offline.html dist/ 2>/dev/null || true",
    "lint": "eslint --config .eslintrc.json js/*.js",
    "test": "node --test tests/*.mjs",
    "deploy": "npm run build && azure-storage-blob upload"
  },
  "keywords": [
    "portfolio",
    "pwa",
    "vanilla-js",
    "homelab",
    "observability"
  ],
  "author": "Eugene Vincent <evince55>",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.57.0",
    "serve": "^14.2.0",
    "three": "^0.160.0"
  },
  "dependencies": {
    "animejs": "^4.5.0"
  }
}
