{
  "name": "career-portal",
  "version": "1.0.0",
  "description": "Content-first portfolio with a live homelab dashboard — vanilla JS PWA with no runtime dependencies",
  "type": "module",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "build": "npm run lint && npm run test && cp -r js css scripts config icons screenshots .github dist/ && cp index.html manifest.json offline.html service-worker.js dist/ 2>/dev/null || true",
    "lint": "eslint js/*.js service-worker.js",
    "test": "node --test tests/*.mjs",
    "deploy": "npm run build && azure-storage-blob upload"
  },
  "keywords": [
    "portfolio",
    "pwa",
    "vanilla-js",
    "homelab",
    "observability"
  ],
  "author": "Eugene Vincent <evince55>",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "globals": "^17.7.0",
    "serve": "^14.2.0"
  },
  "overrides": {
    "brace-expansion": "^5.0.8",
    "fast-uri": "^3.1.4"
  }
}
